<%= simple_form_for(@backlink, html: { class: 'form-horizontal'}) do |f| %>
  <% if @backlink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@backlink.errors.count, "error") %> prohibited this backlink from being saved:</h2>

      <ul>
      <% @backlink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.input :project %>   
  

  
    <%= f.input :project_url %> 
  
  
  <div class="form-group">
    <%= f.input :backlink_url %> 
  </div>
  
  <div class="form-group">
    <%= f.input :method %> 
  </div>
  
  <div class="form-group"> 
    <%= f.input :published_at %>
  </div>
  
  <div class="form-group">
    <%= f.input :notes %> 
  </div>
  
  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>